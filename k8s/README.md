# Kubernetes Manifests

> Prefer the Helm chart at `helm/meme-trader-ai/` for production deployments.
> Raw manifests are generated via: `helm template meme-trader-ai ./helm/meme-trader-ai > k8s/manifests.yaml`
